<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://lorenzoruggerii.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://lorenzoruggerii.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-03-08T18:03:05+00:00</updated><id>https://lorenzoruggerii.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Your Favourite Genomic Model knows more than you think</title><link href="https://lorenzoruggerii.github.io/blog/2025/DiffusionDNABERT-new/" rel="alternate" type="text/html" title="Your Favourite Genomic Model knows more than you think"/><published>2025-12-18T15:12:00+00:00</published><updated>2025-12-18T15:12:00+00:00</updated><id>https://lorenzoruggerii.github.io/blog/2025/DiffusionDNABERT-new</id><content type="html" xml:base="https://lorenzoruggerii.github.io/blog/2025/DiffusionDNABERT-new/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>In the world of Natural Language Processing, we often draw a sharp line between <strong>discriminative</strong> models like BERT (designed to understand and classify) and <strong>generative</strong> models like GPT (designed to create).</p> <p>But what if this boundary is more subtle than you think? What if your favourite BERT-style genomic model could surprise you by generating coherent text just after one hour of finetuning?</p> <p>In this post, we’ll explore how the MLM objective can be mathematically reframed as a <strong>Discrete Diffusion</strong> process, effectively turning a “static” classifier into a powerful generator. I wanted to test this theory using DNABERT-2 to generate synthetic human enhancers and validate them against real ones. All of the code can be found <a href="https://github.com/lorenzoruggerii/DiffusionDNABERT.git">here</a>.</p> <h2 id="masked-language-modeling-mlm">Masked Language Modeling (MLM)</h2> <p>Masked Language Modeling (MLM) is essentially a sophisticated, high-speed game of “fill-in-the-blanks” designed to teach AI models deep contextual understanding. Unlike traditional models that read text sequentially from left to right—predicting the next word based only on what came before—BERT uses MLM to become truly <strong>bidirectional</strong>. It takes a complete sentence and hides 15% of the words, but it does so using a clever <strong>80/10/10 strategy</strong> to prevent the model from becoming lazy.</p> <p>Specifically, for that 15% of chosen words:</p> <ul> <li><strong>80%</strong> are replaced with a <code class="language-plaintext highlighter-rouge">[MASK]</code> token (forcing the model to rely on context).</li> <li><strong>10%</strong> are replaced with a <strong>random word</strong> (forcing the model to act as a “spell checker” that detects logic errors).</li> <li><strong>10%</strong> are left <strong>unchanged</strong> (ensuring the model still values the actual word when it is present).</li> </ul> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/BERT/BERT_model-480.webp 480w,/assets/img/BERT/BERT_model-800.webp 800w,/assets/img/BERT/BERT_model-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/BERT/BERT_model.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Fig: 1. How BERT model behaves.</strong> Random tokens are masked from the input sequence, and the model is asked to identify them using the surrounding context. </div> <p>By forcing the model to analyze these surrounding “clues” from both directions, MLM enables BERT to develop a much richer grasp of language nuances, syntax, and semantics. This is why BERT can easily distinguish between “bank” (a river) and “bank” (a financial institution) based entirely on the words sitting next to it.</p> <h2 id="discrete-diffusion">Discrete Diffusion</h2> <p>Diffusion models are a class of latent variable models that are originally designed for continuous domains. A diffusion model is consisting of a forward diffusion process. Given a sample \(x_{0} \sim q(x_{0})\), a Markov chain of latent variables \(x_{1}, ..., x_{T}\) are produced in the forward process by progressively adding a small amount of Gaussian noise to the sample:</p> <p>\begin{equation} q\left(x_{t} \mid x_{t-1} \right) = \mathcal{N}(x_{t};\sqrt{1 - \beta_{t}}x_{t-1}, \beta_{t}\mathbb{I}) \end{equation}</p> <p>where \(\beta_{t} \in \left(0, 1\right)\) is a noise schedule controlling the step size of adding noise (i.e. the <code class="language-plaintext highlighter-rouge">[MASK]</code> token).</p> <p>It can be shown that if \(\beta_{t}\) is small enaugh, the reverse process \(q\left(x_{t-1} \mid x_{t} \right)\) is also a Gaussian, learned by the parametrized model.</p> <p>\begin{equation} p_{\theta}\left(x_{t-1} \mid x_{t}, t\right) = \mathcal{N}\left(x_{t-1};\mu_{\theta}\left(x_{t}, t\right), \Sigma_{\theta}\left(x_{t}, t\right)\right) \end{equation}</p> <p>where \(\mu_{\theta}\) and \(\Sigma_{\theta}\) can be implemented using a U-Net or a Neural Network. When conditioning also on \(x_{0}, p_{\theta}\left(x_{t-1} \mid x_{t}, x_{0}\right)\) has a closed form so we can use <strong>Kulback-Leider divergence</strong> as a loss for our model.</p> <p>For discrete domains, each element of \(x_{t}\) is a discrete random variable with K categories. Denote \(x_{t}\) as a stack of one-hot vectors, the process of adding noise can be written as:</p> <p>\begin{equation} q_{\left(x_{t} \mid x_{t-1}\right)} = \text{Cat}\left(x_{t}; p = x_{t-1} Q_{t}\right) \end{equation}</p> <p>where \(\text{Cat}(z)\) is a categorical distribution (i.e. the random variable can take one of K possible categories) and \(Q_{t}\) is a transition matrix that is applied to each token in the sequence independently.</p> <p>Therefore, for a given token, using Bayes theorem, it is easy to obtain that:</p> <p>\begin{equation} q\left(x_{t-1} \mid x_{t}, x_{0}\right) = \text{Cat} \big( x_{t-1}; p = \frac{x_{t} Q^{T}_{t} \odot x_{0}\bar{Q}_{t-1}}{x_{0}\bar{Q}_{t}x_{t}^{T}} \big) \end{equation}</p> <p>where \(\bar{Q}_{t} = Q_{1}Q_{2} \dots Q_{t}\). So with \(q\left(x_{t-1} \mid x_{t}, x_{0}\right)\) we can learn to reverse the diffusion process.</p> <h2 id="bert-is-a-one-step-diffusion-model">BERT is a one-step diffusion model</h2> <p>Luckily for us, <a href="https://s-sahoo.com/mdlm">it can be shown</a> that the Loss for a Discrete Diffusion model can be transformed into an MLM loss under the following conditions:</p> <ol> <li><strong>Zero-Masking Probabilities</strong>: a clear input (i.e. an input token which is non-masked) is never masked.</li> <li><strong>Carry-Over Unmasking</strong>: an unmasked token remains unchanged during reverse diffusion.</li> </ol> <p>If so, the loss function can be rewritten as:</p> \[\mathbb{E}_{q} \int_{t=0}^{1} \frac{\alpha_t'}{1 - \alpha_t} \sum_{l} \log p_{\theta}(x_t \mid x_0) \cdot x_0 \, dt\] <p>But this is exactly the <strong>MLM loss function</strong>. So, a BERT-based model can be finetuned to be used as a diffusion model. At each step we replace a different proportion $p$ of tokens with <code class="language-plaintext highlighter-rouge">[MASK]</code> using a variable $p \in (0.10, 0.90)$. In this way, the model is trained to replace <code class="language-plaintext highlighter-rouge">[MASK]</code> tokens with real ones in different conditions. During generation, we start from a completely masked sequence, and, at each denoising step, BERT will replace some proportion <code class="language-plaintext highlighter-rouge">[MASK]</code> with generated tokens, until the full sequence is constructed.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/BERT/roberta-diffusion-480.webp 480w,/assets/img/BERT/roberta-diffusion-800.webp 800w,/assets/img/BERT/roberta-diffusion-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/BERT/roberta-diffusion.gif" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Fig: 2. BERT model diffusion generation.</strong> At each step, some [MASK] tokens gets replaced and the sequence denoised. Taken from: https://nathan.rs/posts/roberta-diffusion/. </div> <h2 id="dnabert-generates-enhancers">DNABERT generates enhancers</h2> <p>Now that we know that we can finetune BERT to produce sequences, let’s try it with DNABERT. <a href="https://academic.oup.com/bioinformatics/article/37/15/2112/6128680">DNABERT</a> is a BERT-based genomic model trained on a huge collection of DNA sequences. It has archieved state-of-the-art results in many genomic tasks, including enhancer prediction, promoter identification, and splice site prediction.</p> <p>To make a choice, I decided to finetune DNABERT-2 with enhancer sequences taken from the Genomic Understanding Evaluation (GUE) benchmark, available on HF <a href="https://huggingface.co/datasets/leannmlindsey/GUE">here</a>. In particular, I selected enhancers coming from the K562 cell line, and divided them into training and test sets.</p> <p>The finetuning procedure follows what written in the preceeding paragraph, i.e. randomly masking a proportion $p$ of tokens from each sequence, with $p$ ranging from $0.10$ to $0.90$.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">collate_fn</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">batch</span><span class="p">,</span> <span class="n">debug</span><span class="p">:</span> <span class="nb">bool</span> <span class="o">=</span> <span class="bp">False</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">]:</span>
        
        <span class="c1"># Tokenize the sequences
</span>        <span class="p">[...]</span>

        <span class="c1"># Extract ids and attention mask
</span>        <span class="n">batch_input_ids</span> <span class="o">=</span> <span class="n">tok_out</span><span class="p">[</span><span class="sh">"</span><span class="s">input_ids</span><span class="sh">"</span><span class="p">]</span>
        <span class="n">batch_attention</span> <span class="o">=</span> <span class="n">tok_out</span><span class="p">[</span><span class="sh">"</span><span class="s">attention_mask</span><span class="sh">"</span><span class="p">]</span>

        <span class="c1"># Get input shape
</span>        <span class="n">B</span><span class="p">,</span> <span class="n">L</span> <span class="o">=</span> <span class="n">batch_input_ids</span><span class="p">.</span><span class="n">shape</span>

        <span class="c1"># Clone input ids for the labels
</span>        <span class="n">labels</span> <span class="o">=</span> <span class="n">batch_input_ids</span><span class="p">.</span><span class="nf">clone</span><span class="p">()</span>

        <span class="c1"># Sample t ~ Uniform[0, 1)
</span>        <span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">empty</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="mi">1</span><span class="p">).</span><span class="nf">uniform_</span><span class="p">(</span><span class="mf">0.10</span><span class="p">,</span> <span class="mf">0.90</span><span class="p">)</span>
        <span class="n">alpha_t</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">alpha</span><span class="p">(</span><span class="n">t</span><span class="p">)</span>
        <span class="n">p</span> <span class="o">=</span> <span class="mi">1</span> <span class="o">-</span> <span class="n">alpha_t</span>

        <span class="c1"># Determine which tokens can be masked
</span>        <span class="n">mask_candidate</span> <span class="o">=</span> <span class="p">(</span><span class="n">batch_attention</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)</span> <span class="o">&amp;</span> <span class="p">(</span><span class="o">~</span><span class="n">is_special</span><span class="p">)</span>

        <span class="c1"># Randomly select tokens to mask based on p
</span>        <span class="n">rand</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">rand_like</span><span class="p">(</span><span class="n">batch_input_ids</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="nb">float</span><span class="p">)</span>
        <span class="n">mask_positions</span> <span class="o">=</span> <span class="p">(</span><span class="n">rand</span> <span class="o">&lt;</span> <span class="n">p</span><span class="p">)</span> <span class="o">&amp;</span> <span class="n">mask_candidate</span>

        <span class="c1"># Apply masking
</span>        <span class="n">batch_input_ids</span><span class="p">[</span><span class="n">mask_positions</span><span class="p">]</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">tokenizer</span><span class="p">.</span><span class="n">mask_token_id</span>

        <span class="c1"># Set unmasked positions to -100 in labels (ignored by loss)
</span>        <span class="n">labels</span><span class="p">[</span><span class="o">~</span><span class="n">mask_positions</span><span class="p">]</span> <span class="o">=</span> <span class="o">-</span><span class="mi">100</span>

        <span class="k">return</span> <span class="p">{</span>
            <span class="sh">"</span><span class="s">input_ids</span><span class="sh">"</span><span class="p">:</span> <span class="n">batch_input_ids</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">attention_mask</span><span class="sh">"</span><span class="p">:</span> <span class="n">batch_attention</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">MLM_labels</span><span class="sh">"</span><span class="p">:</span> <span class="n">labels</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">t</span><span class="sh">"</span><span class="p">:</span> <span class="n">t</span> <span class="c1"># to keep track of timesteps
</span>        <span class="p">}</span> 
</code></pre></div></div> <p>This ensures that the model is able to reconstruct the input sequence for different masking proportions.</p> <p>The model was finetuned for 20 epochs, maybe a little too much (but took less than an hour on my 4060 GPU), and the results are shown below.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/BERT/loss-480.webp 480w,/assets/img/BERT/loss-800.webp 800w,/assets/img/BERT/loss-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/BERT/loss.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Fig: 3. DiffusionDNABERT loss.</strong> Training loss is shown in orange, test loss in red, and token accuracy in violet. Please note that the losses have different ranges from the accuracy (0, 1). </div> <p>Now that we have the model, let’s <strong>start generating something</strong> and see how good are the generated enhancer. But, first of all, how can I decide whether an enhancer is good or bad? And, most importantly, what is an enhancer? Following <em>Barral et al., 2023</em>:</p> <blockquote> <p>Enhancers are cis-regulatory elements that can stimulate gene expression from distance, and drive precise spatiotemporal gene expression profiles during development. Functional enhancers display specific features including an open chromatin conformation, Histone H3 lysine 27 acetylation, Histone H3 lysine 4 mono-methylation enrichment, and enhancer RNAs production. [[…]] Their DNA sequences are composed of tissue-specific transcription factor (TFs) binding sites, conferring tissue specific activity.</p> </blockquote> <p>So, an enhancer is a specific sequence that:</p> <ol> <li>Usually is in an <strong>open-chromatin region</strong> so it must display some transcription factor binding sites.</li> <li>Displays specific features like Histone H3 lysine 27 acetylation (H3K27ac) and Histone H3 lysine 4 mono-methylation (H3K3me1)</li> </ol> <p>So let’s search for TFs and H3K27ac and H3K3me1 traits in our generated sequences. The TFs can be found screening different position frequency matrices (PFMs) against the generated and test sequences to see whether they share the same composition of TFs, while the Histone traits can be found using a DL model like Enformer. For the PFMs, I downloaded the human (redundant) collection from <a href="https://jaspar.elixir.no/downloads/">JASPAR</a>.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/BERT/TF_composition-480.webp 480w,/assets/img/BERT/TF_composition-800.webp 800w,/assets/img/BERT/TF_composition-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/BERT/TF_composition.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Fig: 4. TF composition.</strong> The axes represent the proportion of transcription factor binding sites in the generated sequences (x-axis) and test sequences (y-axis). Both the spearman and pearson correlation are significant (p &lt; 0.05). </div> <p>Unfortunately, the H3K27ac head for K562 cells is not available in Enformer, but we can still measure the H3K3me1 trait. For this experiment, since Enformer context window is roughly 200kbp, I inserted my generated sequences (roughly 3000bp) into 200kbp random sequences, and measured the H3K3me1 trait. Below you can see an example:</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/BERT/seq_5_H3K4me1-480.webp 480w,/assets/img/BERT/seq_5_H3K4me1-800.webp 800w,/assets/img/BERT/seq_5_H3K4me1-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/BERT/seq_5_H3K4me1.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Fig: 5. H3K3me1 trait in a generated sequence.</strong> The inserted generated sequence (injected in the middle of the random sequence) shows a high valued trait compared to the rest of the sequence. </div> <p>So, I decided to create a bunch of 2000 sequences (as many as they are in the test set), measure their activity, to compare with the test set’s activity. In the end, the generated sequences seems to follow the same activity pattern, underlying a good generation.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/BERT/activity_values-480.webp 480w,/assets/img/BERT/activity_values-800.webp 800w,/assets/img/BERT/activity_values-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/BERT/activity_values.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Fig: 6. H3K3me1 trait distribution in generated and test sequences.</strong> The distribution of the H3K3me1 trait values across the generated sequences match and follows the one related to the test set. </div> <h2 id="conclusions">Conclusions</h2> <p>This results seems promising, since in just one hour we passed from having an encoder to have a generative model which generates good human enhancers. So, for synthetic DNA generation, this suggests that instead of starting from scratch, we can take the massive genomic “knowledge” already stored in pre-trained models like DNABERT and “steer” it toward designing synthetic regulatory elements with specific functional goals.</p> <p>However, for now the model is only capable of generating one type of sequences. I’m wondering whether working on the <code class="language-plaintext highlighter-rouge">[CLS]</code> token during generation could steer the model into producing more specific synthetic sequences (i.e. generating promoters or enhancers).</p> <p>Ultimately, this experiment proves that these models are not just static lookup tables for genomic features. When we change our perspective on how to use them, we realize that <strong>your favourite genomic model knows more than you think</strong>.</p>]]></content><author><name></name></author><category term="genomics"/><category term="BERT"/><category term="diffusion"/><summary type="html"><![CDATA[How to generate DNA sequences starting from a BERT-like model?]]></summary></entry><entry><title type="html">Circuit Tracing: An in-depth explanation about creating interpretability circuits</title><link href="https://lorenzoruggerii.github.io/blog/2025/transcoders-new/" rel="alternate" type="text/html" title="Circuit Tracing: An in-depth explanation about creating interpretability circuits"/><published>2025-08-26T00:00:00+00:00</published><updated>2025-08-26T00:00:00+00:00</updated><id>https://lorenzoruggerii.github.io/blog/2025/transcoders-new</id><content type="html" xml:base="https://lorenzoruggerii.github.io/blog/2025/transcoders-new/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>Large language models can often feel like inscrutable black boxes: they generate text with astonishing fluency, but the internal computations that drive their outputs remain hidden from us. Recently, I came across the paper “Transformers Find Interpretable LLM Feature Circuits” <d-cite key="dunefsky2024"></d-cite>, and while the paper is beautifully written, I found myself struggling to grasp every piece of the math and methodology. That’s why I decided to write this blog post: to break down the ideas, explain the math behind circuit finding, and provide a concrete, step-by-step view of how features, attention heads, and layers interact in a transformer.</p> <p>The key idea is to treat a model as a computational graph of interacting components. By tracing attribution scores through this graph, we can identify the contributions of earlier-layer features and heads to later-layer representations, and eventually reconstruct causal circuits inside the model. Along the way, I discuss the theoretical foundation for this approach, show how feature vectors must be updated as we traverse the graph, and present algorithms for discovering important computational paths.</p> <p>To make this more tangible, I finish with a case study on a small transformer trained on TinyStories, a dataset of fairytales. Here, interpretability techniques reveal exactly how the model encodes the familiar opening “Once upon a time”—uncovering the features and attention heads responsible for producing it.</p> <h2 id="what-is-a-transcoder">What is a Transcoder?</h2> <p>Transcoders are a new way of peeking inside the black box of transformers. Traditionally, researches have used sparse autoencoders (SAEs) <d-cite key="cunningham2023"></d-cite> to make sense of model activations, since SAEs break down the hidden states (i.e. the residual stream) of large language models into interpretable “features”. But there’s a catch: while SAEs often uncover meaningful features, they don’t tell us much about how those features flow through the MLP and attention blocks of a transformers. That’s where transcoders come in. A transcoder is trained to imitate an MLP layer’s input-output behavior using a wide, sparsely-activating approximation.  The transcoder architecture allows us to separate features’ attribution into input-dependent and input-invariant components. Crucially, the input-invariance is key - it lets us ask general questions about how features connect and transform, rather than chasing explanations that only apply to specific examples. This gives us a powerful tool for understaning how features interact inside transformers, allowing us to construct interpretability circuits.</p> <h2 id="transcoders-architecture">Transcoder’s Architecture</h2> <p>At its core, a transcoder is a simple but powerful idea: we replace a transformer’s original MLP sublayer with a wider, sparse approximation that is (hopefully) easier to interpret. Concretely, a transcoder is just a one-hidden-layer ReLU MLP that learns to mimic the input-output behavior of the original MLP. Given an input activation vector $x \in \mathbb{R}^{d_{\text{model}}}$ the transcoder computes an intermediate sparse feature vector:</p> \[z_{\text{TC}}(x) = \text{ReLU}({W_{\text{enc}}}x) + b_{\text{enc}}\] <p>where $W_{\text{enc}} \in \mathbb{R}^{d_\text{features} \times d_{\text{model}}}$ maps the input into a higher-dimensional “feature space” ($d_{\text{features}} \gg d_{\text{model}}$) and $b_{\text{enc}}$ is a bias term. Each component of $z_{\text{TC}}(x)$ represents the activation of a sparse feature.</p> <p>The decoder stage then maps these features back into the model’s hidden space:</p> \[\text{TC}(x) = W_{\text{dec}}z_{\text{TC}}(x) + b_{\text{dec}},\] <p>where $W_{\text{dec}} \in \mathbb{R}^{d_{\text{model}} \times {d_{\text{features}}}}$ and $b_{\text{dec}}$ reconstruct the MLP’s output. Intuitively, the encoder decides <em>which features should fire</em>, while the decoder decides <em>how each feature contributes</em> to the final output.</p> <p>To train transcoders, we minimize a loss that balances <strong>faithfulness</strong> to the original MLP with <strong>sparsity</strong> of activations:</p> \[\mathcal{L}_{\text{TC}}(x) = \| \text{MLP}(x) - \text{TC}(x) \|_2^2 + \lambda_1 \| z_{\text{TC}}(x) \|_1\] <p>where the first term ensures the transcoder closedly matches the true MLP outputs, and the second term (with hyperparameter $ \lambda_1 $) encourages sparse activations, making the features easier to interpret.</p> <p>In short, transcoders trade a little extra width for a lot of interpretability: they give us a sparse, feature-level view of how information flows through MLP sublayers, without losing track of the original model’s computations.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/transcoders/transcoders_architecture-480.webp 480w,/assets/img/transcoders/transcoders_architecture-800.webp 800w,/assets/img/transcoders/transcoders_architecture-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/transcoders/transcoders_architecture.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Fig: 1. Difference between SAEs and transcoders</strong>. SAEs learn to reconstruct model activations, whereas transcoders imitate sublayers’ input-output behavior. </div> <h2 id="circuit-tracing">Circuit tracing</h2> <p>With transcoders in hand, we can go a step further and actually trace the circuits that transformers use to solve tasks. The idea is simple: instead of asking how individual neurons connect, we ask how features discovered by transcoders in one layer connect to features in later layers. Suppose we found a feature in layer $\text{l’}$ that activates on certain tokens with a clear pattern (for example, semicolons after a year token <d-footnote>like in (Einstein 1946<strong>;</strong> Feynman 1947<strong>;</strong>)</d-footnote> ). Now we want to know which features fire in all layers $\text{l}, \text{l} \lt \text{l’}$. In other words: what makes that feature fire in layer $\text{l’}$? Is it just the semicolon or there is some information coming from previous tokens? To do this, we treat the model as a graph of computations and try to extract the subgraph that’s truly responsible for that given behavior. That requires a way of scoring each connection: how much do the attention heads and the MLP layers contribute to one feature’s activation?</p> <h3 id="computing-the-attributions-between-transcoder-feature-pairs">Computing the Attributions between Transcoder Feature Pairs</h3> <p>To understand how circuits are build, we start by giving a way of measuring the connection between two transcoder features. With transcoders, this turn out to be extremely simple. Formally, let $z^{(l,i)}_{\text{TC}}(x^{(l,t)}_{\text{mid}})$ denote the scalar activation of the $i$-th feature in the layer $l$ transcoder on token $t$, given the MLP input $x^{(l, t)}_{\text{mid}}$. Then for $l \lt l’$ the contribution of feature $ i $ in transcoder $ l $ to the activation of feature $ i’ $ in transcoder $ l’ $ is:</p> \[z^{(l,i)}_{\text{TC}}\left(x^{(l,t)}_{\text{mid}}\right) \cdot \left( f^{(l,i)}_{\text{dec}} \cdot f^{(l',i')}_{\text{enc}} \right) \tag{1}\] <p>Here:</p> <ul> <li>$z^{(l,i)}_{\text{TC}}(x^{(l,t)}_{\text{mid}})$ is the activation of the earlier feature — this depends on the current input.</li> <li>$f^{(l,i)}_{\text{dec}}\cdot f^{(l’,i’)}_{\text{enc}}$ is the dot product between the earlier feature’s decoder vector and the later feature’s encoder vector — this is input-invariant once the transcoder is trained.</li> </ul> <p>This clean factorization gives us the best of both worlds:</p> <ul> <li>An <strong>input-invariant term</strong> that tells us how features are generally connected across the model.</li> <li>An <strong>input-dependent term</strong> that tells us how much a feature mattered for the specific input at hand.</li> </ul> <p>Now we’ll see how to derive equation 1. We are interested in the activation of feature $ i’ $ in transcoder layer $ l’ $ that activates on token $ t $. The activation is given by:</p> \[z_{\text{TC}}^{(l', i')}(x_{\text{mid}}^{(l', t)}) = \text{ReLU}(f_{\text{enc}}^{(l', i')} \cdot x_{\text{mid}}^{(l', t)} + b_{\text{enc}}^{(l', i')})\] <p>where $f_{\text{enc}}^{(l’, i’)}$ is the $ i-th $ row of $W_{\text{enc}}$ for the layer $ l’ $ transcoder and $b_{\text{enc}}^{(l’, i’)}$ is the learned encoder bias for feature $ i’ $ in the layer $ l’ $ transcoder. Now, we know that this feature is active (i.e. $z_{\text{TC}}^{(l’, i’)}(x_{\text{mid}}^{(l’, t)}) \gt 0$) and it’s reasonable to assume that this firing is not given by the bias term. We can therefore ignore $b_{\text{enc}}^{(l’, i’)}$. Then, since $z_{\text{TC}}^{(l’, i’)}(x_{\text{mid}}^{(l’, t)}) \gt 0$ we can further ignore the $ \text{ReLU} $, leaving us only with:</p> \[z_{\text{TC}}^{(l', i')}(x_{\text{mid}}^{(l', t)}) \approx f_{\text{enc}}^{(l', i')} \cdot x_{\text{mid}}^{(l', t)}\] <p>Thanks to the <em>residual connections</em> in transformers $x_{\text{mid}}^{(l’, t)}$ can be decomposed as the sum of the outputs of all previous components in the model (MLPs and Attention blocks). For instance, in a two-layer model, if $x_{\text{mid}}^{(2, t)}$ is the input for the second MLP sublayer:</p> \[x_{\text{mid}}^{(2, t)} = \sum_{h}\text{attn}^{(2, h)}(x_{\text{pre}}^{(2, t)};x_{\text{pre}}^{(2, 1:t)}) + \text{MLP}(x_{\text{mid}}^{(1, t)}) + \sum_{h}\text{attn}^{(1, h)}(x_{\text{pre}}^{(1, t)};x_{\text{pre}}^{(1, 1:t)})\] <p>Now, assume that we want to consider how much a feature from the first MLP layer contributes to a feature in the second layer. Because of linearity, the amount of $\text{MLP}^{(1)}(x_{\text{mid}}^{(1, t)})$ that contributes to $f_{\text{enc}}^{(2, i’)} \cdot x_{\text{mid}}^{(2, t)}$ is given by:</p> \[f_{\text{enc}}^{(2, i')} \cdot \text{MLP}^{(1)}(x_{\text{mid}}^{(1, t)}) \tag{2}\] <p>This is generalizable for understanding the contribution of $ \text{MLP} \ l $ to the activation of feature $ i’ $ in transcoder $ l’ $, with $ l \lt l’ $. Now, we can start from here to find the attribution of feature $ i $ in transcoder $ l $ with respect to feature $ i’ $ in transcoder $ l’ $.</p> <p>In particular, we know that transcoders are trained to reconstruct MLP outputs. So, if layer $ l $ transcoder is a good approximation to the layer $ l $ MLP, then $\text{MLP}^{(1)}(x_{\text{mid}}^{(l, t)}) \approx \text{TC}^{(l)}(x_{\text{mid}}^{(l, t)})$. Hence, we can use this approximation in Equation 2 to obtain:</p> \[f_{\text{enc}}^{(l', i')} \cdot \text{MLP}^{(l)}(x_{\text{mid}}^{(l, t)}) \approx f_{\text{enc}}^{(l', i')} \cdot \text{TC}^{(l)}(x_{\text{mid}}^{(l, t)}) \tag{3}\] <p>Equation 3 tells us the <strong>whole</strong> contribution of $\text{TC}^{(l)}$ on feature $ i’ $ in transcoder $ l’ $. To extract individual feature attributions, we can leverage transcoder’s linearity $\text{TC}^{(l)}(x_{\text{mid}}^{(l, t)}) = \sum_{\text{feature} \ j} z_{\text{TC}}^{(l, j)}(x_{\text{mid}}^{(l, t)})f_{\text{dec}}^{(l, j)}$. This is how we can decompose the transcoder attribution into its features contributions to feature $ i’ $ in transcoder $ l’ $.</p> <p>Thus, we have:</p> \[f_{\text{enc}}^{(l', i')} \cdot \text{MLP}^{(l)}(x_{\text{mid}}^{(l, t)}) \approx f_{\text{enc}}^{(l', i')} \cdot \sum_{\text{feature} \ j} z_{\text{TC}}^{(l, j)}(x_{\text{mid}}^{(l, t)})f_{\text{dec}}^{(l, j)} \tag{4}\] <p>Therefore, the attribution of feature $ i $ in layer $ l $ with respect to feature $ i’ $ in transcoder $ l’ $ is given by:</p> \[z_{\text{TC}}^{(l, j)}(x_{\text{mid}}^{(l, t)})(f_{\text{enc}}^{(l', i')} \cdot f_{\text{dec}}^{(l, j)}) \tag{5}\] <p>where we have just put $f_{\text{enc}}^{(l’, i’)}$ inside the parenthesis since $z_{\text{TC}}^{(l, j)}(x_{\text{mid}}^{(l, t)})$ is a scalar.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/transcoders/MLP_attrib-480.webp 480w,/assets/img/transcoders/MLP_attrib-800.webp 800w,/assets/img/transcoders/MLP_attrib-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/transcoders/MLP_attrib.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Fig: 2. Attribution between Transcoder Feature Pairs</strong>. Here we are calculating the attribution of feature j in layer l with respect to feature i' in layer l'. The attribution is given by Equation 5. </div> <h3 id="computing-the-attributions-through-attention-heads">Computing the Attributions through Attention Heads</h3> <p>So far, we have focused on tracing how a lower-layer transcoder feature directly contributes to a higher-layer transcoder feature at the same token position. However, transcoder features can also be mediated by <strong>attention heads</strong>, which requires us to extend the analysis. Attention can be decomposed into two circuits, a QK (query-key) circuit, which decides <em>where</em> to move information from and to, and the OV (output-value) circuit, which decides <em>what</em> information to move <d-cite key="elhage2021"></d-cite>. Specifically, we now want to compute the attribution of transcoder features through the <strong>OV (output-value) circuit</strong> of an attention head. To do this, we treat the QK circuit scores as fixed, and only look at the contributions through the OV circuit. As before, our goal is to explain what causes feature $i’$ in the transcoder at layer $l’$ to activate on token $t$. Given an attention head $h$ at layer $l$ (with $l \lt l’$), the same reasoning as in the direct case shows that the head’s contribution to feature $i’$ is given by:</p> \[f^{(l',i')}_{\text{enc}} \cdot \text{attn}^{(l,h)}\big(x^{(l,t)}_{\text{pre}}; x^{(l,1:t)}_{\text{pre}}\big) \tag{6}\] <p>This can be decomposed into a sum over source tokens $s$:</p> \[f^{(l',i')}_{\text{enc}} \cdot \sum_{\text{source token}\,s}\text{score}^{(l,h)}\big(x^{(l,t)}_{\text{pre}}, x^{(l,s)}_{\text{pre}}\big) \, W^{(l,h)}_{\text{OV}}\,x^{(l,s)}_{\text{pre}} \tag{7}\] \[=\sum_{\text{source token}\,s}\text{score}^{(l,h)}\big(x^{(l,t)}_{\text{pre}}, x^{(l,s)}_{\text{pre}}\big) \, \big(f^{(l',i')}_{\text{enc}}\big)^{T}W^{(l,h)}_{\text{OV}}\,x^{(l,s)}_{\text{pre}} \tag{8}\] \[=\sum_{\text{source token}\,s}\text{score}^{(l,h)}\big(x^{(l,t)}_{\text{pre}}, x^{(l,s)}_{\text{pre}}\big) \, \big(\big(W^{(l,h)}_{\text{OV}}\big)^{T}f^{(l',i')}_{\text{enc}}\big) \cdot x^{(l,s)}_{\text{pre}} \tag{9}\] <p>where from (7) to (8) we put $f^{(l’,i’)}_{\text{enc}}$ inside the parenthesis since $\text{score}^{(l,h)}\big(x^{(l,t)}_{\text{pre}}, x^{(l,s)}_{\text{pre}}\big)$ are scalars for all $s$ and from (8) to (9) we used $A\cdot B = (B^{T}\cdot A^{T})^{T}$.</p> <p>Thus, the contribution of source token $s$ at layer $l$ through head $h$ can be written as:</p> \[\text{score}^{(l,h)}\big(x^{(l,t)}_{\text{pre}}, x^{(l,s)}_{\text{pre}}\big) \, \big(\big(W^{(l,h)}_{\text{OV}}\big)^\top f^{(l',i')}_{\text{enc}}\big) \cdot x^{(l,s)}_{\text{pre}} \tag{10}\] <p>So now, for a given feature $i’$ that fires in layer $l’$ on token $t$, we can now trace back <strong>all its influences</strong>. We can calculate the contribution from every previous-layer feature (Equation 5) and from any source token $s$, with $s \lt t$, processed from attention head $h$ (Equation 10).</p> <p>The final step is to note that $x^{(l,s)}_{\text{pre}}$ itself can be decomposed again into the output of MLP sublayers and attention heads, allowing us to recurse back until the token embeddings. To visualize this better, assume that you know that on layer 8 feature 2020 is firing on token 5. You apply Equation (5) and you find that most of the contribution is coming from feature 1546 in layer 4, activating on token 5. Your next question is: why is feature 1546 activating? So, given equations 5 and 10, you can recurse back, constructing a computational graph, until you get to the first layer of your model, in which you can see the tokens that make your feature fire. In the next subsection, I’ll explain how to get the attribution graph.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/transcoders/attn_attrib-480.webp 480w,/assets/img/transcoders/attn_attrib-800.webp 800w,/assets/img/transcoders/attn_attrib-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/transcoders/attn_attrib.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Fig: 3. Attribution through attention heads</strong>. Here we are calculating the attribution of token s through attention head h in layer l with respect to feature i' in layer l'. The attribution is given by Equation 10. </div> <h3 id="tracing-features-back">Tracing Features Back</h3> <p>So far, we’ve seen how to calculate the attribution from an earlier-layer transcoder feature or attention head to a later-layer feature. But that’s only half of the story. To truly understand the model’s behavior, we need to flip the perspective: <strong>what contributes to these earlier-layer features or heads in the first place?</strong></p> <p>This bring us to the idea of <strong>recursive attribution tracing</strong>, that can be expressed as follows:</p> <blockquote> <p>Starting from the final feature vector we care about (e.g. $f_{enc}^{(l’, i’)}$ for $i$-th feature in layer $l’$) we move backwards step by step. At each node - earlier transcoder feature or attention head - we compute how much that node contributes to the current feature vector using Equations 5 and 10. We than update our feature vector accordingly and continue tracing backwards until we reach the inputs.</p> </blockquote> <p>Now, why do we need to update the feature vector step? Recall that we are calculating attributions for a particular feature in a given layer computing the inner product beween its <strong>feature vector</strong> and its inputs $f_{\text{enc}}^{(l’, i’)} \cdot x_{\text{mid}}^{(l, t)}$. So the feature vector represents the lens thruogh with we measure attribution, and that lens changes as activations are transformed along the computational path <d-footnote>for example, traversing a linear layer</d-footnote>. If we held the same vector fixed everywhere, we’d be ignoring how each node remaps information. Consider a simple example: suppose a node $c$ takes an input $x$ and produces an output $y=Ax$ where $A$ is a weight matrix. If our current feature is $f’$, then the attribution of $c$ is $a’ = f’ \cdot y$ and assume that this attribution is significant. We therefore want to trace back again to understand what causes $c$ to activate. However, here it’s not enough to reuse $f’$ directly, because $f’$ “lives” in the output space of $y$ not in the input space of $x$. Instead, we update the feature vector so that the original $a’$ attribution gets untouched:</p> \[a' = f' \cdot y = f' \cdot (Ax) = (f' \cdot A)x = f \cdot x \tag{11}\] <p>So our new feature feature vector will be $f=f’\cdot A$. This way, $f$ is aligned with the input space and the attribution remains valid one step earlier in the graph. The same principles applies for MLP and Attention nodes that we have in the graph: the feature vector <strong>must be updated to remain consistent</strong> with the representation space of the preceding layer.</p> <p>So, in order to compute the new feature vector $f$ through node $c$ starting from $f’$ we’ll use the following contraint given from Equation 11:</p> \[a' = f' \cdot y = f \cdot x\] <p>When node $c$ is an attention head in layer $l$, we have $y = x_{\text{mid}}^{(l, t)}$, so:</p> \[a' = f' \cdot y = f' \cdot x_{\text{mid}^{(l, t)}} = f' \cdot \sum_{h}\sum_{s}\text{score}^{(l,h)}\big(x^{(l,t)}_{\text{pre}}, x^{(l,s)}_{\text{pre}}\big) \, W^{(l,h)}_{\text{OV}}\,x^{(l,s)}_{\text{pre}}\] <p>However, assume that we are tracing the attribution through head $h$ of a source token at position $s$. Therefore, we have $x = x_{\text{pre}}^{(l, s)}$ and the updated feature vector will be:</p> \[a' = f' \cdot \text{score}^{(l,h)}\big(x^{(l,t)}_{\text{pre}}, x^{(l,s)}_{\text{pre}}\big) \, W^{(l,h)}_{\text{OV}}\,x^{(l,s)}_{\text{pre}}\] \[= f' \cdot \text{score}^{(l,h)}\big(x^{(l,t)}_{\text{pre}}, x^{(l,s)}_{\text{pre}}\big) \, W^{(l,h)}_{\text{OV}}\,x\] \[= \big(f' \cdot \text{score}^{(l,h)}\big(x^{(l,t)}_{\text{pre}}, x^{(l,s)}_{\text{pre}}\big) \, W^{(l,h)}_{\text{OV}}\big)\,x\] \[= f \cdot x\] <p>Hence:</p> \[f = f' \cdot \text{score}^{(l,h)}\big(x^{(l,t)}_{\text{pre}}, x^{(l,s)}_{\text{pre}}\big) \, W^{(l,h)}_{\text{OV}} \tag{12}\] <p>In contrast, if $c$ is a transcoder feature then $y = x_{\text{post}}^{(l, t)}$, so we have:</p> \[a' = f' \cdot x_{\text{post}}^{(l, t)} = f' \cdot \text{MLP}^{(l)}(x_{\text{mid}}^{(l, t)}) \approx f' \cdot \text{TC}^{(l)}(x_{\text{mid}}^{(l, t)})\] \[= f' \cdot \sum_{j}z_{\text{TC}}^{(l, j)}(x_{\text{mid}}^{(l, t)}) \cdot f_{\text{dec}}^{(l, j)}\] <p>Moreover, from the definition of transcoder, we have that $z_{\text{TC}}^{(l, t)}(x_{\text{mid}}^{(l, t)}) = f_{\text{enc}}^{(l, j)} \cdot (x_{\text{mid}}^{(l, t)})$. Therefore, we obtain:</p> \[a' = f' \cdot x_{\text{post}}^{(l, t)} = f' \cdot \sum_{j} f_{\text{enc}}^{(l, j)} \cdot (x_{\text{mid}}^{(l, t)}) \cdot f_{\text{dec}}^{(l, j)}\] <p>From this, we can just insert inside the sum $f’$ thanks to the linearity of the inner product:</p> \[a' = f' \cdot x_{\text{post}}^{(l, t)} = f' \cdot \sum_{j} f_{\text{enc}}^{(l, j)} \cdot (x_{\text{mid}}^{(l, t)}) \cdot f_{\text{dec}}^{(l, j)}\] \[=\sum_{j} \big(f_{\text{enc}}^{(l, j)} \cdot (x_{\text{mid}}^{(l, t)})\big) \cdot \big( f' \cdot f_{\text{dec}}^{(l, j)}\big)\] <p>And from this, knowing that our $x = (x_{\text{mid}}^{(l, t)})$ and assuming we are interested in the contribution of feature $j$ from layer $l$ it’s easy to see that:</p> \[f = \big( f' \cdot f_{\text{dec}}^{(l, j)}\big) \cdot f_{\text{enc}}^{(l, j)} \tag{13}\] <p>There is, however, an important caveat. Transformer architectures insert a LayerNorm operation before every MLP and attention sublayer. This nonlinearity complicates attribution because it rescales and recenters activations. Fortunately, prior work provides intuition and justification for approximating LayerNorm as a linear scaling transformation. Neel Nanda <a href="https://www.neelnanda.io/mechanistic-interpretability/attribution-patching">here</a> argues that it behaves roughly like multiplying the input by a constant, and Dunefsky &amp; Cohan <d-cite key="dunefsky2024a"></d-cite> provide both theoretical motivation and empirical evidence supporting this view. In practice, this means that when we update the feature vector $f$ at each sublayer, we multiply it by an empirically estimated <em>LayerNorm scaling constant</em> defined as the ratio between the norm of the pre-LayerNorm activation vector and the post-LayerNorm activation vector.</p> <p>At this point, we’re able to trace back our feature vector in any block of our transformer model. In other words, we’re ready to <strong>construct the attribution graph</strong>!</p> <h3 id="computing-the-attribution-graph">Computing the Attribution Graph</h3> <p>The full circuit-finding algorithm is basically a way of “tracing back the breadcrumbs” to figure out which parts of a transformer are truly responsible for a feature lighting up. Imagine you notice a neuron firing strongly in a late layer — the algorithm asks: what earlier features, attention heads, or even embeddings made this happen? To answer that, it builds computational paths step by step, moving backwards through the network. At each step, it looks at all possible contributors, scores them by how much they mattered, and then keeps only the top few. This greedy approach means we don’t get lost in the exponential explosion of possible paths, but we still hang onto the most important ones. After running this process for several steps, you end up with a collection of “candidate stories” — paths that explain how the signal was built. But paths alone can be messy: the same node might appear in several different explanations, and if we just add everything up we’d double-count. That’s why the second stage of the algorithm takes all those paths and merges them into a single graph. In this graph, every node and every edge gets an attribution score that represents its total contribution across all explanations. And just to stay honest, we also allow for “error nodes” — placeholders that capture the fact that we didn’t include every possible path or that our approximations (like treating LayerNorm as linear) aren’t perfect. The end result is a clear, interpretable circuit diagram showing how the model built up the feature of interest.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/transcoders/circuit_tracing-480.webp 480w,/assets/img/transcoders/circuit_tracing-800.webp 800w,/assets/img/transcoders/circuit_tracing-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/transcoders/circuit_tracing.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Fig: 4. Full-circuit-finding algorithm</strong>. Taken from (Dunefsky et al., 2025) </div> <h3 id="an-example-using-fairytales">An example using fairytales</h3> <p>To make things more concrete, let’s look at an example drawn from a model I recently trained on TinyStories, a dataset of short fairytales (you can find it <a href="https://github.com/lorenzoruggerii/TinyTranscoder">here</a>). One of the most characteristic phrases in this dataset is the classic opening “Once upon a time”. The model reproduces this phrase frequently, and I was curious to see where in the network this pattern is “stored” and how it emerges.</p> <p>To start, I probed the activations inside the model when it processed this phrase. Looking specifically at the token “a” in “Once upon a time”, I found that feature 1932 in layer 1 showed particularly strong activation.</p> <div class="l-page"> <iframe src="/assets/plotly/1932.html" frameborder="0" scrolling="no" height="500px" width="100%" style="border: 1px dashed grey;"></iframe> </div> <div class="caption"> <strong>Fig: 5. Feature 1932 activations</strong>. Activations of feature 1932 in 30 different generated sequences. Note how this feature activates on "a" when following "Once upon ". </div> <p>Interestingly, whenever this feature lights up, the model becomes strongly biased towards predicting the token “time” as the next word. This suggests that feature 1932 has learned to specialize in carrying forward the “Once upon a … time” motif.</p> <p>To validate this hypothesis, I ran an intervention: I manually activated feature 1932 in contexts where it would not normally fire. Here, no matter the surrounding text, the model tried to output “time” immediately afterward.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/transcoders/time-480.webp 480w,/assets/img/transcoders/time-800.webp 800w,/assets/img/transcoders/time-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/transcoders/time.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Fig: 6. Effect of Feature 1932 on prompt "This is a "</strong>. See how the model shiftes completely its predictions once feature 1932 is activated. Just by activating that group of neurons in layer 1, the model wants to output "time" with 99% confidence. </div> <p>This confirms that the feature does not simply correlate with the training distribution, but actually plays a causal role in driving the model toward producing this phrase.</p> <p>Going deeper, I also used the circuit-finding method described earlier to trace which components contribute to the activation of feature 1932. Two particularly clear computational paths emerged:</p> <ul> <li>Path 1: <code class="language-plaintext highlighter-rouge">mlp1@2@2 ← mlp0tc[2915]@2: 2.8 ← attn0[3]@0: 0.24</code></li> <li>Path 2: <code class="language-plaintext highlighter-rouge">mlp1@2@2 ← mlp0tc[2915]@2: 2.8 ← attn0[2]@1: 0.21</code></li> </ul> <p>This notation means that feature 1932 in layer 1 is being modulated by <strong>feature 2915 in layer 0</strong>, which itself receives contributions from <strong>attention head 3 at token position 0</strong> and <strong>attention head 2 at token position 1</strong>. In plain language: the feature that predicts <em>“time”</em> is activated through a chain that depends on the tokens <em>“once”</em> and <em>“upon”</em>.</p> <p>Altogether, this paints a coherent picture. The model has carved out a small, interpretable circuit that detects the prefix <em>“once upon a …”</em> and channels this information forward to bias the output toward <em>“time”</em>. In other words, the phrase <em>“Once upon a time”</em> has been distilled into a <strong>mechanistic substructure</strong> inside the model.</p> <p>This example highlights the promise of feature- and circuit-level interpretability: even in a small two-layer transformer, we can begin to see how specific linguistic motifs are encoded, propagated, and ultimately expressed in the model’s predictions.</p> <h2 id="conclusions">Conclusions</h2> <p>By following attributions backward through the network and systematically building computational paths into circuits, we gain a concrete picture of how language models organize knowledge internally. What initially appears as an opaque “black box” turns into a structured system of features, heads, and layers that cooperate to produce meaningful outputs.</p> <p>The case study on “Once upon a time” illustrates this beautifully: a specific feature in layer 1, modulated by lower-layer components attending to “once” and “upon”, reliably pushes the model to predict “time”. This shows that the model has not just memorized text, but carved out specialized circuits to handle recurring linguistic motifs.</p> <p>Of course, this is just one small example in a tiny model. The ultimate challenge is to scale these methods to much larger architectures and more abstract concepts, where the circuits will be deeper and more intertwined. Still, even in this setting, we see a glimpse of what is possible: by peeling back the layers of the network, we can begin to map out the hidden structures that give rise to language understanding.</p>]]></content><author><name>Lorenzo Ruggeri</name></author><category term="Mechanistic Interpretability"/><category term="AI"/><category term="Transformers"/><summary type="html"><![CDATA[How do we extract interpretability circuits from transcoders?]]></summary></entry><entry><title type="html">Mambacoders find interpretable Mamba feature circuits</title><link href="https://lorenzoruggerii.github.io/blog/2025/mambacoder/" rel="alternate" type="text/html" title="Mambacoders find interpretable Mamba feature circuits"/><published>2025-08-26T00:00:00+00:00</published><updated>2025-08-26T00:00:00+00:00</updated><id>https://lorenzoruggerii.github.io/blog/2025/mambacoder</id><content type="html" xml:base="https://lorenzoruggerii.github.io/blog/2025/mambacoder/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>Mechanistic interpretability has made impressive strides on transformers — we now have sparse autoencoders, transcoders, and circuit-tracing algorithms that can identify human-interpretable features and trace how they interact. But the landscape of sequence modeling is shifting. Mamba has emerged as a compelling alternative to transformers, offering linear-time inference and competitive language modeling performance. More importantly, a growing number of frontier models are hybrid architectures <d-cite key="Nvidia2025"></d-cite>, interleaving Mamba layers with attention blocks, and if we want to understand these models mechanistically, we need interpretability tools that work for SSM layers too. This post introduces MambaCoders: transcoders adapted for Mamba mixer blocks. I’ll show how to train them, how to derive attribution formulas that account for Mamba’s selective scan dynamics, and how to trace circuits across SSM heads. As a proof of concept, I’ll walk through a fully interpretable, causally verified “hunger” circuit discovered in a small Mamba model trained on TinyStories: a concrete example of mechanistic interpretability working end-to-end on a pure SSM.</p> <h2 id="what-is-a-transcoder">What is a Transcoder?</h2> <p>If you want to deep dive, check out my <a href="https://lorenzoruggerii.github.io/blog/2025/transcoders-new/">previous post on transcoders</a>. In short: a transcoder <d-cite key="dunefsky2024"></d-cite> is a sparse, wide one-hidden-layer MLP trained to imitate a model sublayer’s input-output behavior. Given an input $x \in \mathbb{R}^{d_{\text{model}}}$, it comprises a sparse feature vector $z_{\text{TC}}(x) = \text{ReLU}({W_{\text{enc}}}x) + b_{\text{enc}}$ and reconstruct the sublayer’s output as $\text{TC}(x) = W_{\text{dec}}z_{\text{TC}}(x) + b_{\text{dec}}$. Compared to sparse autoencoders <d-cite key="cunningham2023"></d-cite> (SAEs), which decompose hidden states but don’t explain how features <strong>flow</strong> through a layer, transcoders give us a feature-level view of the actual computation, making them a powerful tool for constructing interpretability circuits.</p> <h2 id="mamba-ssm">Mamba SSM</h2> <p>To understand MambaCoders, we first need to understand the architecture they are designed to interpret. Transformers have dominated sequence modeling for years, but they come with a well-known cost: attention scales quadratically with sequence length, making long-context modeling expensive both in compute and memory. State Space Models (SSMs) emerged as a promising alternative, drawing inspiration from classical control theory. An SSM maps an input sequence $x(t)$ through a latent state $h(t)$ via a linear recurrence:</p> \[h_{t} = \bar{A}h_{t-1} + \bar{B}x_{t}, \ y_{t} = Ch_{t}\] <p>where $\bar{A}$ and $\bar{B}$ are the discretized versions of continuous-time parameters. Crucially, this recurrence can also be computed as a convolution during training, giving SSMs the best of both worlds: parallelizable training and efficient $\mathcal{O}(1)$-per-step autoregressive inference (with constant memory overhead). Early SSMs like S4 <d-cite key="gu2021"></d-cite> demonstrated strong results on long-range benchmarks, but struggled on discrete, information-dense data like text. The issue lied in their weights: their parameters are fixed across time steps (a property called <em>linear time invariance</em>, or LTI) preventing them from performing content-aware reasoning.</p> <p>Mamba <d-cite key="gu2023"></d-cite> addressed this limitation with a <strong>selection mechanism</strong>: rather than keeping $\bar{A}$, $\bar{B}$, $C$ fixed across time, they are made functions of the current input $x_{t}$, turning the time-invariant recurrence into a time-varying one:</p> \[h_{t} = \bar{A}_{t} h_{t-1} + \bar{B}_{t} x_{t} \\ y_{t} = C_{t} h_{t}\] <p>where $ \bar{A}<em>{t}, \bar{B}</em>{t}, C_{t} $ are now all linear functions of the input $f ( x_{t} )$. This input dependence is what gives the model its name<d-footnote>S6, standing for S4 with a <strong>S</strong>election mechanism computed via a <strong>S</strong>can</d-footnote> and is what allows it to selectively propagate or forget information depending on the content of the current token, rather than treating all inputs uniformly. This breaks LTI, and therefore Mamba cannot be view from a convolutional point of view, but since its state update is linear, outputs can be computed in parallel via a parallel scan <d-cite key="blelloch1989"></d-cite>. This overcomes the efficiency challenges avoiding materializing the large intermediate state in GPU HBM.</p> <h2 id="the-mamba-block">The Mamba Block</h2> <p>The S6 layer sits inside a larger <strong>Mamba block</strong> (Figure 1), which wraps the selective SSM with projections and a gating mechanism.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/MambaCoder/MambaBlock-480.webp 480w,/assets/img/MambaCoder/MambaBlock-800.webp 800w,/assets/img/MambaCoder/MambaBlock-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/MambaCoder/MambaBlock.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Fig: 1. The Mamba Block</strong>. The mamba block is composed by a time-mixing and a channel-mixing path, combined through an elementwise product. The time-mixing path contains a short convolution </div> <p>Given a residual stream input $x \in \mathbb{R}^{d_{\text{model}}}$, the block proceeds as follows. First, the input is projected into a higher-dimensional space via two parallel linear projections:</p> \[u = W_{\text{in}}x, \ g = W_{\text{gate}}x\] <p>where both $u, v \in \mathbb{R}^{\text{ED}}$ with $E$ being the expansion factor. The $u$ branch is then passed through a short convolution (which seems to introduce asymmetries <d-cite key="Chen2026"></d-cite> and substituted in Mamba-3 <d-cite key="mamba3"></d-cite>) followed by a SiLU activation before entering the SSM:</p> \[u' = \text{SiLU}(\text{Conv1d}(u))\] <p>The S6 parameters $B_{t}$, $C_{t}$ and $\Delta_{t}$ are then computed as linear projections of $u’$, implementing the selection mechanism. The SSM is applied to produce:</p> \[y = \text{S6}(u')\] <p>The output is then gated by the second branch through an elementwise product with a SiLU-activated gate, before a final output projection brings it back to the residual stream dimension:</p> \[z = y \odot \text{SiLU}(g) \\ \text{out} = W_{\text{in}}z\] <p>The full block is stacked homogeneoysly, interleaved with RMSNorm and residual connections, forming the Mamba architecture. Compared to a Transformer layer, which separates attention and MLP into two distinct sublayers, the Mamba block fuses sequence mixing and nonlinear transformation into a single unit, which is both more parameter-efficient and harder to decompose with standard interpretability tools.</p> <p>The catch, from an interpretability standpoint, is that Mamba’s computational structure is fundamentally different from transformers. There are no attention heads to probe, and information flows through a compressed hidden state governed by learned, input projections. That’s exactly what we want to accomplish here: reformulate transcoder’s theory to apply it to Mamba models.</p> <h2 id="how-to-apply-transcoders-to-mamba">How to Apply Transcoders to Mamba?</h2> <p>Applying transcoders to transformers is relatively straightforward: since the MLP sublayer has a clean input-output interface sitting alongside the attention sublayer, one can simply train a transcoder to mimic the MLP’s input-output behavior in isolation. The two sublayers are additive and largely independent, which makes the decomposition natural.</p> <p>Mamba breaks this assumption. As described above, the Mamba block fuses sequence mixing (the SSM) and channel mixing (the gated projections) into a single unit. There is no clean MLP sublayer to target, since the two blocks interact in a nonlinear way.</p> <p>The solution is to treat the <strong>entire Mamba block</strong> as the unit of transcoding: I trained the MambaCoder to take the block’s input from the residual stream and predict its output, bypassing the internal complexity. Formally, if $x_{l}$ is the residual stream at layer $l$ before the Mamba block, the MambaCoder learns:</p> \[\text{MC}_{l}(x_{l}) \approx \text{MambaBlock}_{l}(x_{l})\] <p>This is the same transcoder objective as in the transformer case, just applied at a coarser granularity.</p> <p>There is one sublety worth flagging: because the Mamba block contains both temporal mixing (SSM) and channel mixing (SSM), any feature interaction in the last mixing layer is not explicitly captured by the transcoder’s feature decomposition, since we won’t be able to tell whether that feature comes from the temporal or channel axis. In practice, I found this to be a minor issue, since most of the information between tokens is passed before the last layer, expecially in deeper models.</p> <h2 id="computing-attribution-for-mamba-layers">Computing Attribution for Mamba Layers</h2> <p>Once we have trained MambaCoders for each layer, we can use them to trace how features propagate through the model. Attribution works differently depending on whether we are connecting two MambaCoder features across layers, or connecting a MambaCoder feature back to a specific Mamba mixer head and token.</p> <h3 id="attribution-between-mambacoder-features">Attribution Between MambaCoder Features</h3> <p>Recall that the MambaCoder at layer $l$ approximates the Mamba block’s output as a sparse sum over features:</p> \[\text{MC}^{(l)}(x^{(l, t)}) \approx \sum_{j} z_{\text{TC}}^{(l, j)}(x^{(l, t)})f_{dec}^{(l, j)}\] <p>where $z_{\text{TC}}^{(l, j)}$ is the activation of feature $j$ at layer $l$ and token $t$, and $f_{dec}^{(l, j)}$ is its decoder direction in the residual stream.</p> <p>Now suppose we want to measure how much feature $j$ in layer $l$ contributed to the activation of feature $i’$ in a later layer $l’ \gt l$. The encoder of the later MambaCoder reads from the residual stream, so the contribution of the decoder direction $f_{dec}^{(l, j)}$ to the activation of feature $i’$ is simply their dot product, weighted by the activation strength:</p> \[\text{attr}(l, j) \to (l', i') = z_{\text{TC}}^{(l, j)}(x^{(l, t)}) \cdot (f_{enc}^{(l', i')} \cdot f_{dec}^{(l, j)})\] <p>This is the standard transcoder attribution formula, and it works the same way here as in the transformer case.</p> <h3 id="attribution-from-a-mambacoder-feature-to-a-mamba-mixer-head">Attribution from a MambaCoder Feature to a Mamba Mixer Head</h3> <p>The more interesting case is attributing a MambaCoder feature in layer $l’$ back to the <strong>Mamba mixer</strong> in an earliear layer $l$. This requires us to unroll the SSM recurrence and reason about which token positions contributed to the current hidden state.</p> <p>Recall that Mamba selective scan computes, for each output token $t$:</p> \[y_{t} = C_{t}h_{t} = \sum_{s=1}^{t} C_{t} \prod_{j=s}^{t}\bar{A}^{j} \bar{B}_{s} x^{(l, s)}\] <p>This has the form of a weighted sum over all past tokens, and we can define an attention-like matrix <d-cite key="Ali2024"></d-cite> $\alpha$ whose $(t, s)$ entry captures the correlation between output token $t$ and input token $s$.</p> \[\alpha_{t, s} = C_{t} \prod_{j=s}^{t}\bar{A}^{j} \bar{B}_{s}\] <p>However, this scalar picture only holds for a single SSM dimension. In Mamba, there is one independent SSM per input dimension $d$, so the full model has $D$ such “heads”, one per channel. We can treat each input dimension $h \in {1, \dots, D}$ as a separate Mamba attention head, in direct analogy with multi-head attention in transformers.</p> <h3 id="deriving-the-attribution-formula">Deriving the Attribution Formula</h3> <p>We want to compute the contribution of Mamba head $h$ at token $s$ in layer $l$ to the activation of encoder feature $i’$ in layer $l’$. We need to evaluate:</p> \[f_{enc}^{(l', i')} \cdot \text{stack}_{h} (\sum_{s=1}^{t} C_{t}^{(h)} \prod_{j=s}^{t}\bar{A}^{j} \bar{B}_{s} x^{(l, s)}[h])\] <p>where $ \text{stack}<em>{h} $ assembles the scalar SSM outputs across all heads into a single $D$-dimensional vector. Defining $s(h) = sum</em>{s=1}^{t} C_{t}^{(h)} \prod_{j=s}^{t}\bar{A}^{j} \bar{B}<em>{s} x^{(l, s)}[h]$ and writing the stack as $ \text{stack}</em>{h} ( s(h) ) = \sum_{h} s(h)e(h)$, where $e(h)$ is the $h$-th standard basis vector, we get:</p> \[f_{enc}^{(l', i')} \cdot \text{stack}_{h}(s(h)) = \sum_{h=1}^{D} s(h) \cdot f_{enc}^{(l', i')}[h]\] <p>Substituting back and exchanging the order of summation:</p> \[= \sum_{s=1}^{t} \sum_{h=1}^{D} sum_{s=1}^{t} C_{t}^{(h)} \prod_{j=s}^{t}\bar{A}^{j} \bar{B}_{s} x^{(l, s)}[h] \cdot f_{enc}^{(l', i')}[h]\] <p>Fixing a specific head $h$ and source token $s$, the attribution from Mamba layer $l$, head $h$, token $s$ to feature $i’$ at token $t$ in layer $l’$ is therefore:</p> \[\text{attr}((l, h, s) \to (l', i', t)) = C_{t}^{(h)} \prod_{j=s}^{t}\bar{A}^{j} \bar{B}_{s} x^{(l, s)}[h] \cdot f_{enc}^{(l', i')}[h]\] <p>With the new feature vector that will be:</p> \[f' = C_{t}^{(h)} \prod_{j=s}^{t}\bar{A}^{j} \bar{B}_{s} \cdot f_{enc}^{(l', i')}\] <h3 id="handling-the-convolution-and-gate">Handling the Convolution and Gate</h3> <p>The attribution formula above atttributes a MambaCoder feature back to the residual stream input $x^{(l, s)}$ of the Mamba block. However, as described in the Mamba block section, the residual stream input does not flow directly into the S6 layer: it first passes through a short <strong>convolution</strong>, and after the SSM the output is multiplied elementwise by a <strong>SiLU-activated gate</strong>. When tracing features back through the full circuit, we need to account for both of these operations.</p> <p>Handling them precisely would require unrolling the convolution kernel and the gate into the attribution computation, significantly complicating the circuit tracing. Instead, I treat both the convolution and the gate as <strong>pointwise scaling operations</strong> on the feature vectors. I know that this is a brute approximation, but it holds when the dominant source of token mixing in Mamba is the S6 selective scan, not the short local convolution.</p> <p>Concretely, for a given layer $l$ and token $s$, we compute the scaling factor of each operation as the ration of activations before and after it:</p> \[\gamma_{\text{conv}^{(l, s)}} = \frac{\text{act_after_conv}^{(l,s)}}{\text{act_before_conv}^{(l,s)}}, \quad \gamma_{\text{gate}^{(l, s)}} = \frac{\text{act_after_gate}^{(l,s)}}{\text{act_before_gate}^{(l,s)}}\] <p>and then rescale the feature vector accordingly as we trace it back toward the embeddings:</p> \[f' = f' \odot \gamma_{\text{gate}}^{(l, s)} \odot \gamma_{\text{conv}}^{(l, s)}\] <p>While the conv could give us some attributions in the time domain, the gate is applied channel wise, so it’s okay to apply scaling to each dimension.</p> <h2 id="an-hungry-example">An “Hungry” Example</h2> <p>To put everything together, I applied MambaCoders to a concrete case to examine whether they could extract some intresting features. I trained a simple 4-layer pure Mamba model on the TinyStories dataset, then trained MambaCoders on each of its layers. What follows is the story of one particuarly interpretable feature: <strong>feature 3498 in layer 1</strong>.</p> <h3 id="discovering-the-feature">Discovering the Feature</h3> <p>The first hint came from a simple experiment. Given the prompt <em>“I’m so”</em>, the model confidently predicts emotional words, like <em>excited, glad, happy</em>. But given <em>“I like to eat toasts for breakfast. I’m so</em>, something changes: the top prediction shifts to <strong>hungry</strong>, and feature 3498 in layer 1 fires strongly on the last token.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/MambaCoder/discovering_the_feature-480.webp 480w,/assets/img/MambaCoder/discovering_the_feature-800.webp 800w,/assets/img/MambaCoder/discovering_the_feature-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/MambaCoder/discovering_the_feature.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Fig. 2.</strong> For the prompt "I'm so", only a generic layer 1 feature fires (left). Adding food context activates feature 3498 in layer 1, shifting the prediction to "hungry" (right). </div> <p>To verify that this feature is causally responsible, and not just correlated, I artificially activated feature 3498 on the <em>“I’m so”</em> prompt and observed the effect on the output distribution. The result is intresting:</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/MambaCoder/hungry_next_tok-480.webp 480w,/assets/img/MambaCoder/hungry_next_tok-800.webp 800w,/assets/img/MambaCoder/hungry_next_tok-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/MambaCoder/hungry_next_tok.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Fig. 3.</strong> Artificially activating feature 3498 on the prompt "I'm so" shifts the model's top prediction from "excited" to "hungry". </div> <h3 id="what-does-the-feature-represent">What Does the Feature Represent?</h3> <p>To understand what feature 3498 is actually encoding, I projected its decoder direction together with the token embedding vectors of the tokenizer into a 3D space using UMAP:</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/MambaCoder/umap_tokens-480.webp 480w,/assets/img/MambaCoder/umap_tokens-800.webp 800w,/assets/img/MambaCoder/umap_tokens-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/MambaCoder/umap_tokens.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Fig. 4.</strong> 3D UMAP projection of normalized token embeddings. The feature vector (red arrow) points directly into a cluster of food-related tokens: "fork", "bowl", "eating", "ate", "eat", "food", "hungry", "delicious". </div> <p>The feature vector lands squarely in a cluster of food-related tokens: <em>fork, bowl, eating, ate, eat, food, hungry, delicious</em>. This is not a coincidence — the feature has learned to represent the concept of food and hunger as a direction in the residual stream.</p> <h3 id="steering-the-model-with-the-feature">Steering the Model with the Feature</h3> <p>To further validate the feature’s semantics, I ran a set of generation experiments: I gave <em>“Once Upon a Time”</em> as a prompt to my model and I generated completions both with and without feature 3498 artificially activated. The results are telling:</p> <table> <thead> <tr> <th style="text-align: left">Without feature</th> <th style="text-align: left">With feature</th> </tr> </thead> <tbody> <tr> <td style="text-align: left"><em>there was a little girl named Lily. She loved to play outside…</em></td> <td style="text-align: left"><em>there was a little girl named Lily. She had a yummy dinner with her food. Her…</em></td> </tr> <tr> <td style="text-align: left"><em>there were two friends called Jack and Stop…</em></td> <td style="text-align: left"><em>a mummy was cooking. She was baking a cake inside…</em></td> </tr> <tr> <td style="text-align: left"><em>there was a little girl who liked to skip…</em></td> <td style="text-align: left"><em>there was a little girl who loved to eat her dinner. Every night she would eat dinner and</em></td> </tr> <tr> <td style="text-align: left"><em>a girl named Jane was in her garden with her dad. Jane was feeling serious about the sunshine…</em></td> <td style="text-align: left"><em>a little girl named Anna was in her kitchen. She wanted to help her mom. A…</em></td> </tr> </tbody> </table> <p>Every time the feature is active, the model steers the story toward food, cooking, or eating, regardless of the original context. Feature 3498 is a <strong>food/hunger</strong> feature.</p> <h3 id="tracing-the-circuit">Tracing the Circuit</h3> <p>Having identified the feature, I used the circuit tracing algorithm to ask: where does this feature come from in the prompt <em>“I like to eat toasts for breakfast. I’m so”</em>?</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/MambaCoder/the_hungry_circuit-480.webp 480w,/assets/img/MambaCoder/the_hungry_circuit-800.webp 800w,/assets/img/MambaCoder/the_hungry_circuit-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/MambaCoder/the_hungry_circuit.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Fig. 5.</strong> The traced circuit for feature 3498 at the last token. The feature is activated by mixer head 920 in layer 0, which routes information from token 3 ("eat") all the way to the final token position. </div> <p>The circuit tracer finds the following path:</p> <p><code class="language-plaintext highlighter-rouge">block1@-1 -&gt; mixer0[920]@3: 0.22 -&gt; embed0@3: 1.5</code></p> <p>In plain English: feature 3498 in layer 1 at the last token is primarily driven by <strong>head 920 of the layer 0 mixer</strong>, which in turn is reading from <strong>token 3</strong> — the token <em>“eat”</em> — in the input. And the feature vector being passed through that head is precisely the <strong>embedding of the word “eat”</strong> itself. The model has learned to route the semantic content of the word <em>“eat”</em> across the sequence via a specific SSM head, accumulate it into a food/hunger feature in layer 1, and use that feature to condition the next-token prediction.</p> <p>So this demonstrated that MambaCoders can recover human-interpretable features in Mamba-based architectures.</p> <h2 id="conclusions">Conclusions</h2> <p>MambaCoders show that the core goals of mechanistic interpretability (finding human-interpretable features and understanding how they connect) are achievable in Mamba-based architectures. The “hunger” circuit is a small but concrete proof of concept: a single food/hunger feature, causally verified, traced all the way back to the embedding of the word “eat” being routed across the sequence by a specific SSM head. But the broader motivation is what excites me most. Hybrid models mixing Mamba and attention layers are becoming increasingly common at the frontier, and understanding them mechanistically requires tools that can handle both layer types and trace circuits across them. MambaCoders provide the missing piece for the SSM side of that picture. Scaling this approach to larger hybrid models, and combining it with existing transformer interpretability tools, feels like a genuinely promising direction and I hope this post is a small step toward getting there.</p>]]></content><author><name>Lorenzo Ruggeri</name></author><category term="Mechanistic Interpretability"/><category term="AI"/><category term="Mamba"/><category term="SSMs"/><summary type="html"><![CDATA[Transcoders adapted to Mamba models]]></summary></entry><entry><title type="html">Just a moment…</title><link href="https://lorenzoruggerii.github.io/blog/2025/just-a-moment/" rel="alternate" type="text/html" title="Just a moment…"/><published>2025-02-20T00:00:00+00:00</published><updated>2025-02-20T00:00:00+00:00</updated><id>https://lorenzoruggerii.github.io/blog/2025/just-a-moment</id><content type="html" xml:base="https://lorenzoruggerii.github.io/blog/2025/just-a-moment/"><![CDATA[]]></content><author><name></name></author></entry></feed>