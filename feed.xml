<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://lorenzoruggerii.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://lorenzoruggerii.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-08-27T13:03:23+00:00</updated><id>https://lorenzoruggerii.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Circuit Tracing: An in-depth explanation about creating interpretability circuits</title><link href="https://lorenzoruggerii.github.io/blog/2025/transcoders-new/" rel="alternate" type="text/html" title="Circuit Tracing: An in-depth explanation about creating interpretability circuits"/><published>2025-08-26T00:00:00+00:00</published><updated>2025-08-26T00:00:00+00:00</updated><id>https://lorenzoruggerii.github.io/blog/2025/transcoders-new</id><content type="html" xml:base="https://lorenzoruggerii.github.io/blog/2025/transcoders-new/"><![CDATA[<h2 id="introduction">Introduction</h2> <h2 id="transcoders">Transcoders</h2> <p>Transcoders <d-cite key="dunefsky2024"></d-cite> are a new way of peeking inside the black box of transformers. Traditionally, researches have used sparse autoencoders (SAEs) <d-cite key="cunningham2023"></d-cite> to make sense of model activations, since SAEs break down the hidden states (i.e. the residual stream) of large language models into interpretable “features”. But there’s a catch: while SAEs often uncover meaningful features, they don’t tell us much about how those features flow through the MLP and attention blocks of a transformers. That’s where transcoders come in. A transcoder is trained to imitate an MLP layer’s input-output behavior using a wide, sparsely-activating approximation.  The transcoder architecture allows us to separate features’ attribution into input-dependent and input-invariant components. Crucially, the input-invariance is key - it lets us ask general questions about how features connect and transform, rather than chasing explanations that only apply to specific examples. This gives us a powerful tool for understaning how features interact inside transformers, allowing us to construct interpretability circuits.</p> <h2 id="transcoders-architecture">Transcoder’s Architecture</h2> <p>At its core, a transcoder is a simple but powerful idea: we replace a transformer’s original MLP sublayer with a wider, sparse approximation that is (hopefully) easier to interpret. Concretely, a transcoder is just a one-hidden-layer ReLU MLP that learns to mimic the input-output behavior of the original MLP. Given an input activation vector $ x \in \mathbb{R}^{d_{\text{model}}} $ the transcoder computes an intermediate sparse feature vector:</p> \[z_{\text{TC}}(x) = \text{ReLU}({W_{\text{enc}}}x) + b_{\text{enc}}\] <p>where $ W<em>{\text{enc}} \in \mathbb{R}^{d</em>\text{features} \times d<em>{\text{model}}} $ maps the input into a higher-dimensional “feature space” ($ d</em>{\text{features}} \gg d<em>{\text{model}} $) and $ b</em>{\text{enc}} $ is a bias term. Each component of $ z_{\text{TC}}(x) $ represents the activation of a sparse feature.</p> <p>The decoder stage then maps these features back into the model’s hidden space:</p> \[\text{TC}(x) = W_{\text{dec}}z_{\text{TC}}(x) + b_{\text{dec}},\] <p>where $ W<em>{\text{dec}} \in \mathbb{R}^{d</em>{\text{model}} \times {d<em>{\text{features}}}} $ and $ b</em>{\text{dec}} $ reconstruct the MLP’s output. Intuitively, the encoder decides <em>which features should fire</em>, while the decoder decides <em>how each feature contributes</em> to the final output.</p> <p>To train transcoders, we minimize a loss that balances <strong>faithfulness</strong> to the original MLP with <strong>sparsity</strong> of activations:</p> \[\mathcal{L}_{\text{TC}}(x) = \| \text{MLP}(x) - \text{TC}(x) \|_2^2 + \lambda_1 \| z_{\text{TC}}(x) \|_1\] <p>where the first term ensures the transcoder closedly matches the true MLP outputs, and the second term (with hyperparameter $ \lambda_1 $) encourages sparse activations, making the features easier to interpret.</p> <p>In short, transcoders trade a little extra width for a lot of interpretability: they give us a sparse, feature-level view of how information flows through MLP sublayers, without losing track of the original model’s computations.</p> <h2 id="circuit-tracing">Circuit tracing</h2> <p>With transcoders in hand, we can go a step further and actually trace the circuits that transformers use to solve tasks. The idea is simple: instead of asking how individual neurons connect, we ask how features discovered by transcoders in one layer connect to features in later layers. Suppose we found a feature in layer $ \text{l’} $ that activates on certain tokens with a clear pattern (for example, semicolons after a year token <d-footnote>like in (Einstein 1946<strong>;</strong> Feynman 1947<strong>;</strong>)</d-footnote> ). Now we want to know which features fire in all layers $ \text{l}, \text{l} \lt \text{l’} $. In other words: what makes that feature fire in layer $ \text{l’} $? Is it just the semicolon or there is some information coming from previous tokens? To do this, we treat the model as a graph of computations and try to extract the subgraph that’s truly responsible for that given behavior. That requires a way of scoring each connection: how much do the attention heads and the MLP layers contribute to one feature’s activation?</p> <h3 id="computing-the-attributions-between-transcoder-feature-pairs">Computing the Attributions between Transcoder Feature Pairs</h3> <p>To understand how circuits are build, we start by giving a way of measuring the connection between two transcoder features. With transcoders, this turn out to be extremely simple. Formally, let $z^{(l,i)}<em>{\text{TC}} (x^{(l,t)}</em>{\text{mid}})$ denote the scalar activation of the $ \text{i-th} $ feature in the layer $ l $ transcoder on token $ t $, given the MLP input $ x^{(l, t)}_{\text{mid}} $. Then for $ l \lt l’ $ the contribution of feature $ i $ in transcoder $ l $ to the activation of feature $ i’ $ in transcoder $ l’ $ is:</p> \[z^{(l,i)}_{\text{TC}}\!\left(x^{(l,t)}_{\text{mid}}\right) \cdot \left( f^{(l,i)}_{\text{dec}} \cdot f^{(l',i')}_{\text{enc}} \right) \tag{1}\] <p>Here:</p> <ul> <li>$z^{(l,i)}<em>{\text{TC}} (x^{(l,t)}</em>{\text{mid}})$ is the activation of the earlier feature — this depends on the current input.</li> <li>$f^{(l,i)}<em>{\text{dec}} \cdot f^{(l’,i’)}</em>{\text{enc}}$ is the dot product between the earlier feature’s decoder vector and the later feature’s encoder vector — this is input-invariant once the transcoder is trained.</li> </ul> <p>This clean factorization gives us the best of both worlds:</p> <ul> <li>An <strong>input-invariant term</strong> that tells us how features are generally connected across the model.</li> <li>An <strong>input-dependent term</strong> that tells us how much a feature mattered for the specific input at hand.</li> </ul> <p>Now we’ll see how to derive equation 1. We are interested in the activation of feature $ i’ $ in transcoder layer $ l’ $ that activates on token $ t $. The activation is given by:</p> \[z_{\text{TC}}^{(l', i')}(x_{\text{mid}}^{(l', t)}) = \text{ReLU}(f_{\text{enc}}^{(l', i')} \cdot x_{\text{mid}}^{(l', t)} + b_{\text{enc}}^{(l', i')})\] <p>where $f_{\text{enc}}^{(l’, i’)}$ is the $ i-th $ row of $W_{\text{enc}}$ for the layer $ l’ $ transcoder and $b_{\text{enc}}^{(l’, i’)}$ is the learned encoder bias for feature $ i’ $ in the layer $ l’ $ transcoder. Now, we know that this feature is active (i.e. $z_{\text{TC}}^{(l’, i’)}(x_{\text{mid}}^{(l’, t)}) \gt 0$) and it’s reasonable to assume that this firing is not given by the bias term. We can therefore ignore $b_{\text{enc}}^{(l’, i’)}$. Then, since $z_{\text{TC}}^{(l’, i’)}(x_{\text{mid}}^{(l’, t)}) \gt 0$ we can further ignore the $ \text{ReLU} $, leaving us only with:</p> \[z_{\text{TC}}^{(l', i')}(x_{\text{mid}}^{l', t}) \approx f_{\text{enc}}^{(l', i')} \cdot x_{\text{mid}}^{(l', t)}\] <p>Thanks to the <em>residual connections</em> in transformers $x_{\text{mid}}^{(l’, t)}$ can be decomposed as the sum of the outputs of all previous components in the model (MLPs and Attention blocks). For instance, in a two-layer model, if $x_{\text{mid}}^{(2, t)}$ is the input for the second MLP sublayer:</p> \[x_{\text{mid}}^{(2, t)} = \sum_{h}\text{attn}^{(2, h)}(x_{\text{pre}}^{(2, t)};x_{\text{pre}}^{(2, 1:t)}) + \text{MLP}(x_{\text{mid}}^{(1, t)}) + \sum_{h}\text{attn}^{(1, h)}(x_{\text{pre}}^{(1, t)};x_{\text{pre}}^{(1, 1:t)})\] <p>Now, assume that we want to consider how much a feature from the first MLP layer contributes to a feature in the second layer. Because of linearity, the amount of $\text{MLP}^{(1)}(x_{\text{mid}}^{(1, t)})$ that contributes to $f_{\text{enc}}^{(2, i’)} \cdot x_{\text{mid}}^{(2, t)}$ is given by:</p> \[f_{\text{enc}}^{(2, i')} \cdot \text{MLP}^{(1)}(x_{\text{mid}}^{(1, t)}) \tag{2}\] <p>This is generalizable for understanding the contribution of $ \text{MLP} \ l $ to the activation of feature $ i’ $ in transcoder $ l’ $, with $ l \lt l’ $. Now, we can start from here to find the attribution of feature $ i $ in transcoder $ l $ with respect to feature $ i’ $ in transcoder $ l’ $.</p> <p>In particular, we know that transcoders are trained to reconstruct MLP outputs. So, if layer $ l $ transcoder is a good approximation to the layer $ l $ MLP, then $\text{MLP}^{(1)}(x_{\text{mid}}^{(l, t)}) \approx \text{TC}^{(l)}(x_{\text{mid}}^{(l, t)})$. Hence, we can use this approximation in Equation 2 to obtain:</p> \[f_{\text{enc}}^{(l', i')} \cdot \text{MLP}^{(l)}(x_{\text{mid}}^{(l, t)}) \approx f_{\text{enc}}^{(l', i')} \cdot \text{TC}^{(l)}(x_{\text{mid}}^{(l, t)}) \tag{3}\] <p>Equation 3 tells us the <strong>whole</strong> contribution of $\text{TC}^{(l)}$ on feature $ i’ $ in transcoder $ l’ $. To extract individual feature attributions, we can leverage transcoder’s linearity $\text{TC}^{(l)}(x_{\text{mid}}^{(l, t)}) = \sum_{\text{feature} \ j} z_{\text{TC}}^{(l, j)}(x_{\text{mid}}^{(l, t)})f_{\text{dec}}^{(l, j)}$. This is how we can decompose the transcoder attribution into its features contributions to feature $ i’ $ in transcoder $ l’ $.</p> <p>Thus, we have:</p> \[f_{\text{enc}}^{(l', i')} \cdot \text{MLP}^{(l)}(x_{\text{mid}}^{(l, t)}) \approx f_{\text{enc}}^{(l', i')} \cdot \sum_{\text{feature} \ j} z_{\text{TC}}^{(l, j)}(x_{\text{mid}}^{(l, t)})f_{\text{dec}}^{(l, j)} \tag{4}\] <p>Therefore, the attribution of feature $ i $ in layer $ l $ with respect to feature $ i’ $ in transcoder $ l’ $ is given by:</p> \[z_{\text{TC}}^{(l, j)}(x_{\text{mid}}^{(l, t)})(f_{\text{enc}}^{(l', i')} \cdot f_{\text{dec}}^{(l, j)}) \tag{5}\] <p>where we have just put $f_{\text{enc}}^{(l’, i’)}$ inside the parenthesis since $z_{\text{TC}}^{(l, j)}(x_{\text{mid}}^{(l, t)})$ is a scalar.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/transcoders/MLP_attrib-480.webp 480w,/assets/img/transcoders/MLP_attrib-800.webp 800w,/assets/img/transcoders/MLP_attrib-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/transcoders/MLP_attrib.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Fig: 1. Attribution between Transcoder Feature Pairs</strong>. Here we are calculating the attribution of feature j in layer l with respect to feature i' in layer l'. The attribution is given by Equation 5. </div> <h3 id="computing-the-attributions-through-attention-heads">Computing the Attributions through Attention Heads</h3> <h3 id="tracing-features-back">Tracing Features Back</h3> <h3 id="computing-the-attribution-graph">Computing the Attribution Graph</h3> <h3 id="an-example-using-fairytales">An example using fairytales</h3> <h2 id="conclusions">Conclusions</h2>]]></content><author><name>Lorenzo Ruggeri</name></author><category term="Mechanistic Interpretability"/><category term="AI"/><category term="Transformers"/><summary type="html"><![CDATA[How do we extract interpretability circuits from transcoders?]]></summary></entry><entry><title type="html">Deciphering Transcription Factor Binding Sites with Wavelet Transforms and Deep Learning | by Lorenzo Ruggeri | Medium</title><link href="https://lorenzoruggerii.github.io/blog/2025/deciphering-transcription-factor-binding-sites-with-wavelet-transforms-and-deep-learning-by-lorenzo-ruggeri-medium/" rel="alternate" type="text/html" title="Deciphering Transcription Factor Binding Sites with Wavelet Transforms and Deep Learning | by Lorenzo Ruggeri | Medium"/><published>2025-02-20T00:00:00+00:00</published><updated>2025-02-20T00:00:00+00:00</updated><id>https://lorenzoruggerii.github.io/blog/2025/deciphering-transcription-factor-binding-sites-with-wavelet-transforms-and-deep-learning--by-lorenzo-ruggeri--medium</id><content type="html" xml:base="https://lorenzoruggerii.github.io/blog/2025/deciphering-transcription-factor-binding-sites-with-wavelet-transforms-and-deep-learning-by-lorenzo-ruggeri-medium/"><![CDATA[<p>Sign upSign inSign upSign in–ListenShareTranscription factors (TFs) play a crucial role in gene regulation by binding to specific DNA sequences known as transcription factor binding sites (TFBS). Accurately identifying these sites is essential for understanding gene expression and its impact on cellular functions. Traditional computational approaches, such as position weight matrices (PWMs) and support vector machines (SVMs), have been widely used for TFBS prediction.In this article, I explore an alternative method that combines wavelet transforms with deep learning to classify TFBS. By transforming DNA sequences into visual representations, we can leverage powerful image classification models like ResNet50 to enhance prediction accuracy and gain deeper insights into TF-DNA interactions.The code to replicate the analysis can be found at:github.comTranscription factors (TFs) are proteins that play a crucial role in regulating gene expression. They do this by binding to specific DNA sequences known as transcription factor binding sites (TFBS). These binding sites are typically short, conserved sequences (called motifs) located in the promoter or enhancer regions of genes. By binding to these sites, transcription factors can either activate or repress the transcription of associated genes, thereby controlling the production of proteins essential for various cellular processes.Understanding the locations and mechanisms of transcription factor binding to DNA is essential for decoding the complex regulatory networks that control cellular functions. Furthermore, accurately identifying potential transcription factor binding sites (TFBS) can contribute to the development of personalized treatments. For instance, consider a scenario in which a genetic variant in an oncogene alters the neighboring DNA sequence, creating a novel TFBS that could potentially initiate oncogenesis. However, experimentally identifying these binding sites is both challenging and time-consuming. Computational methods provide a powerful alternative, enabling faster and more scalable predictions of TFBS, thereby facilitating more efficient biological analysis and medical advancements.Typically, PWM-based models like MEME and STREME, as well as SVM-based models, are used to address this problem. PWM models rely on statistical methods, while SVM models utilize machine learning to identify motifs within positive sequences (i.e., sequences containing TFBS). In this article, I explored an alternative approach by applying wavelet transforms and deep learning to replicate these results for the CTCF transcription factor.One of the most extensively studied transcription factors is the CCCTC-binding factor (CTCF), a highly conserved protein essential for chromatin architecture organization. CTCF primarily functions as an insulator, preventing interactions between enhancers and promoters to ensure proper gene expression. However, in some cases, it also facilitates the formation of chromatin loops, bringing distant regulatory elements into proximity with their target genes and enhancing gene expression.Since CTCF is a key player in gene regulation, it’s a go-to for TFBS prediction studies — so I figured it was the perfect choice for this experiment!Wavelet transforms are mathematical tools that decompose signals into different frequency components, allowing for the analysis of localized variations within the signal. Unlike Fourier transforms, which provide only frequency information, wavelet transforms offer both frequency and temporal information, making them particularly useful for analyzing non-stationary signals.By overlapping a mother wavelet M scaled by a factor σ and shifted by a factor Δ onto a given signal S, we compute the inner product between M(σ, Δ) and S. This operation is then repeated for a range of shifts A* and scales B*.For more details on wavelet transforms, I suggest you to read the following article.medium.comThe wavelet transform generates a set of coefficients for a given signal, which can be structured in a matrix-like form. This reveals a powerful feature of wavelet transforms: by computing the modulus of the complex numbers in the matrix, we obtain an intensity matrix. By mapping these intensities to colors, we can create a colormap — essentially converting a signal into an image! This transformation enables us to leverage the vast array of pre-trained image classification models, such as ResNets, to classify signals based on their colormaps.However, since we’re working with DNA sequences, we first need to determine how to convert DNA strings into signals. There are many ways to convert DNA sequences into signals, all of which fall under the field of Genomic Signal Processing. For this experiment, I decided to go with the easiest possible mapping: each nucleotide (A, C, G, T) corresponds to a specific integer (1, 2, 3, 4).The full pipeline can be visualized in the image below:The ResNet50 model, short for Residual Network with 50 layers, is a deep convolutional neural network (CNN) architecture renowned for its effectiveness in image classification tasks. Introduced by Microsoft Research in 2015, ResNet50 addresses the challenge of vanishing gradients in very deep networks through the use of skip connections or residual blocks. These connections allow gradients to flow directly through the network, enabling the training of much deeper models without losing performance. ResNet50 has become a benchmark in computer vision due to its ability to learn hierarchical features from images, making it highly suitable for tasks like object classification.ResNet50 weights were frozen except for the last convolutional block (Layer4) and the final fully connected layer.ResNet50 was trained on 20,000 images over 20 epochs, delivering strong results with a peak accuracy of 77%, precision of 80%, and recall of 80% in spotting TFBS images compared to background sequences, just after the first two epochs. What’s really cool is that each epoch took less than two minutes to run! That’s a big deal because other models can take up to 30 minutes to handle the same amount of data.So far, I haven’t performed any hyperparameter tuning. However, exploring this could be an interesting way to improve the results.Grad-CAM (Gradient-weighted Class Activation Mapping) is an explainability technique used to visualize which regions of an input image influence a deep learning model’s predictions. It works by analyzing the gradients flowing into the final convolutional layer of a CNN, identifying the most activated neurons for each object class. This produces a heatmap-like representation that highlights the key regions of the image responsible for its classification into a specific label.For a more in-depth explanation about Grad-CAM, I suggest the following article:medium.comBy examining the activations in the three convolutional layers of Layer4, it appears that ResNet50 primarily focuses on the central region of the sequences to identify positive samples. This is a significant pattern, as transcription factor binding sites (TFBS) are typically located near the center of positive sequences, expecially for CTCF.Interestingly, I observed a similar trend when using SVMs to classify the same CTCF-positive sequences. In this case, the SVM assigned higher weights to k-mers (subsequences of length k) located near the sequence center — precisely where motifs are expected to be.This study explored an alternative approach to transcription factor binding site (TFBS) prediction by leveraging wavelet transforms and deep learning, specifically using ResNet50. By converting DNA sequences into signal-based colormaps, we were able to take advantage of pre-trained image classification models, demonstrating strong performance in distinguishing TFBS from background sequences.The results showed that ResNet50 could quickly learn to recognize relevant patterns, achieving a peak accuracy of 77% within just two epochs. Additionally, explainability techniques like Grad-CAM provided valuable insights into how the model focused on the central region of sequences — where TFBS are typically located!While these initial results are promising, there is still room for improvement. Future work could explore hyperparameter tuning, alternative genomic signal processing techniques for DNA encoding, or applying wavelet transforms for other exiting DNA related tasks.—-HelpStatusAboutCareersPressBlogPrivacyRulesTermsText to speech</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Transcription factors (TFs) play a crucial role in gene regulation by binding to specific DNA sequences known as transcription factor binding sites (TFBS). Accurately identifying these sites is…]]></summary></entry></feed>